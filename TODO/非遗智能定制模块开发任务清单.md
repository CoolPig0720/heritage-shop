# 非遗智能定制模块开发任务清单

最后更新：2026-02-10

## 阶段一：数据库设计与配置

### 1.1 表结构落地
- [x] 新增 `ai_image_record` 建表 SQL（backend/src/main/resources/sql/ai_image_record.sql）
- [x] 索引：`idx_user_id(user_id)`
- [ ] 如需扩展分页排序与筛选：增加 `create_time`、`status` 等字段与索引

### 1.2 参数与枚举规范
- [ ] 如需任务化：明确 `generate_type` 与 `status` 的取值与落库字段
- [x] resolution 以腾讯侧格式透传（如 `1024:1024`）
- [x] strength 范围校验（0~1，仅图生图传入时生效）

---

## 阶段二：后端实体层

### 2.1 实体类（Entity）
- [x] 创建 `AiImageRecord` 实体类

### 2.2 Mapper（MyBatis-Plus）
- [x] 创建 `AiImageRecordMapper`（继承 BaseMapper）

---

## 阶段三：后端业务逻辑层

### 3.1 DTO / VO 设计
- [x] 创建 `AiTextToImageRequest`（prompt/resolution/seed/style/rspImgType）
- [x] 创建 `AiImageGenerateResponse`（requestId/resultImage/resultImages/seed/seeds）
- [x] 创建 `AiImageRecordVO`（id/originalImageUrl/prompt/resultImageUrl）

### 3.2 Client 封装（腾讯混元）
- [x] 在 `AiImageServiceImpl` 中封装腾讯云 SDK 调用与错误映射
- [ ] 如需解耦：抽取 `TencentHunyuanClient`（统一封装文生图/图生图调用）

### 3.3 Storage 封装
- [x] 图生图参考图落本地 `uploads/ai-ref` 并拼接可访问 URL（依赖 `file.public-base-url`）
- [ ] 抽象 `StorageService`（参考图、结果图统一落盘）
- [ ] 实现 COS 版本（生产部署使用）

### 3.4 Service 层
- [x] 创建 `AiImageService`（文生图/图生图）
- [x] 创建 `AiImageRecordService`（写入与分页查询历史记录）
- [x] 实现 prompt-only 与 image+prompt 两种模式分流
- [ ] 实现并发控制与额度限制（按用户/全局）

---

## 阶段四：后端控制器层

### 4.1 智能定制接口（登录用户）
- [x] 创建 `AiImageController`
  - [x] POST `/api/customize/ai-image/text-to-image` - 文生图（JSON）
  - [x] POST `/api/customize/ai-image/image-to-image` - 图生图（multipart/form-data）
  - [x] GET `/api/customize/ai-image/records` - 历史记录分页（当前用户）

---

## 阶段五：前端 API 封装

### 5.1 API 接口封装
- [x] 创建 `frontend/src/api/aiImage.js`
  - [x] imageToImage(formData)
  - [x] textToImage(data)
  - [x] pageAiImageRecords(params)

---

## 阶段六：前端页面开发

### 6.1 智能定制页（对应 /customize）
- [x] 升级 `Customize.vue`：prompt 输入（必填）+ 参考图上传（可选）+ 参数面板
- [x] 结果展示区：网格展示 + 放大预览 + 下载
- [x] 状态展示：生成中/成功/失败（失败展示错误原因）
- [x] 历史记录区：分页列表（对话框）+ 点击预览
- [ ] 历史记录分组展示：按 `original_image_url` 是否为空分为文生图/图生图

---

## 阶段七：测试与优化

### 7.1 功能测试
- [ ] 测试 prompt-only 生成
- [ ] 测试 图生图（上传参考图）生成
- [ ] 测试多种 size/n/strength 参数
- [ ] 测试历史记录列表与详情

### 7.2 异常与边界测试
- [ ] 测试空 prompt / 超长 prompt
- [ ] 测试非图片文件 / 超大图片
- [ ] 测试并发超限与每日额度超限
- [ ] 测试腾讯侧失败时错误提示与任务状态落库

### 7.3 性能与工程化
- [ ] 生成任务异步化（线程池/队列），避免接口超时
- [ ] 结果图落盘（COS）防止临时 URL 过期
- [ ] 增加管理员侧统计与风控（可选）

---

## 变更记录

### 2026-02-09
- 对齐当前实现：表结构脚本已落地（ai_image_record.sql），实体/Mapper/Service/Controller/前端 API 均已实现
- 更新接口路径：改为 `/api/customize/ai-image/*`（文生图/图生图/历史记录）
- 更新参数口径：以 `resolution/count/strength` 为主；任务化相关 `status/generate_type` 暂未落库

### 2026-02-10
- 增加待办：前端按 `original_image_url` 分组展示文生图/图生图历史记录（不改表）
