# 后端接口清单表

（基于 Spring Boot 的非遗产品智能定制商城系统）

最后更新：2026-04-17

---

## 一、认证与用户模块接口

---

### 1.1 注册与登录模块

| 接口名称 | 请求方式 | 接口路径               | 参数说明                                   | 访问权限 |
| ---- | ---- | ------------------ | -------------------------------------- | ---- |
| 用户注册 | POST | `/api/auth/register` | account, password, confirmPassword, name, role | 所有人  |
| 用户登录 | POST | `/api/auth/login`    | account, password                          | 所有人  |

---

### 1.2 个人中心模块

| 接口名称     | 请求方式 | 接口路径                 | 参数说明                        | 访问权限                    |
| -------- | ---- | -------------------- | --------------------------- | ----------------------- |
| 获取当前用户信息 | GET  | `/api/user/profile`  | 无                           | USER / MERCHANT / ADMIN |
| 修改个人信息   | PUT  | `/api/user/profile`  | name, avatar, password（可选） | USER / MERCHANT / ADMIN |
| 修改密码     | PUT  | `/api/user/password` | oldPassword, newPassword, confirmPassword | USER / MERCHANT / ADMIN |

---

### 1.3 用户管理模块（管理员）

| 接口名称       | 请求方式   | 接口路径                 | 参数说明                 | 访问权限 |
| ---------- | ------ | -------------------- | -------------------- | ---- |
| 用户列表查询     | GET    | `/api/user/list`     | page, size, name, role | ADMIN |
| 更新用户信息     | PUT    | `/api/user/{id}/update` | name, avatar, password（可选） | ADMIN |
| 更新用户信息（兼容） | PUT    | `/api/user/{id}/status` | name, avatar, password（可选） | ADMIN |
| 删除用户       | DELETE | `/api/user/{id}`     | id                   | ADMIN |

---

## 二、商品管理模块接口

---

### 2.1 商品管理（管理员 / 商家）

| 接口名称   | 请求方式   | 接口路径                        | 参数说明                                                     | 访问权限             |
| ------ | ------ | --------------------------- | -------------------------------------------------------- | ---------------- |
| 新增商品   | POST   | `/api/products`             | name, description, price（可选）                               | ADMIN / MERCHANT |
| 修改商品   | PUT    | `/api/products/{id}`        | name, description, price, status, traceCode, traceQrUrl, model3dUrl（均可选） | ADMIN / MERCHANT |
| 删除商品   | DELETE | `/api/products/{id}`        | id                                                       | ADMIN / MERCHANT |
| 商品列表查询 | GET    | `/api/products`             | page, size, keyword, name, status（status 可选）              | ADMIN / MERCHANT |
| 商品上下架  | PUT    | `/api/products/{id}/status` | status                                                   | ADMIN / MERCHANT |
| 商品图片列表 | GET    | `/api/products/{id}/images` | id                                                       | ADMIN / MERCHANT |
| 新增商品图片 | POST   | `/api/products/{id}/images` | imageUrls[], setFirstAsCover（可选）                          | ADMIN / MERCHANT |
| 更新商品图片 | PUT    | `/api/products/images/{imageId}` | imageUrl, sortOrder, isCover（均可选）                         | ADMIN / MERCHANT |
| 删除商品图片 | DELETE | `/api/products/images/{imageId}` | imageId                                                  | ADMIN / MERCHANT |

> 📌 商家操作时需校验商品归属，管理员可操作所有商品。

---

### 2.2 商城展示模块（前台）

| 接口名称   | 请求方式 | 接口路径                  | 参数说明          | 访问权限 |
| ------ | ---- | --------------------- | ------------- | ---- |
| 随机推荐商品 | GET  | `/api/shop/recommend` | count（可选，默认 20） | USER / MERCHANT / ADMIN |
| 商品详情   | GET  | `/api/shop/products/{id}` | id            | USER / MERCHANT / ADMIN |

---

## 三、实名认证模块接口

---

| 接口名称     | 请求方式 | 接口路径                      | 参数说明                   | 访问权限                    |
| -------- | ---- | ------------------------- | ---------------------- | ----------------------- |
| 提交实名认证   | POST | `/api/certificate`        | certificateNumber, name | USER / MERCHANT / ADMIN |
| 获取当前用户认证信息 | GET  | `/api/certificate`        | 无                      | USER / MERCHANT / ADMIN |
| 更新认证信息   | PUT  | `/api/certificate`        | certificateNumber, name | USER / MERCHANT / ADMIN |
| 获取证件信息   | GET  | `/api/certificate/{certificateNumber}` | certificateNumber      | USER / MERCHANT / ADMIN |

---

## 四、收货地址模块接口

---

| 接口名称     | 请求方式   | 接口路径                         | 参数说明                                                                 | 访问权限                    |
| -------- | ------ | ---------------------------- | -------------------------------------------------------------------- | ----------------------- |
| 新增地址     | POST   | `/api/user/address`          | receiverName, receiverPhone, regionNamePath, regionCodePath（可选）, regionDepth（可选）, detailAddress, isDefault（可选） | USER / MERCHANT / ADMIN |
| 修改地址     | PUT    | `/api/user/address/{id}`     | receiverName（可选）, receiverPhone（可选）, regionNamePath（可选）, regionCodePath（可选）, regionDepth（可选）, detailAddress（可选） | USER / MERCHANT / ADMIN |
| 删除地址     | DELETE | `/api/user/address/{id}`     | id                                                                   | USER / MERCHANT / ADMIN |
| 查询当前用户地址 | GET    | `/api/user/address/list`     | 无                                                                    | USER / MERCHANT / ADMIN |
| 设为默认地址   | PUT    | `/api/user/address/{id}/default` | id                                                                   | USER / MERCHANT / ADMIN |

> 📌 说明：user_id 从 JWT 中获取，禁止前端传入；地区使用 regionNamePath（/ 分隔）存储以支持多级行政区与海外。

---

## 五、购物车模块接口

---

| 接口名称     | 请求方式   | 接口路径                    | 参数说明 | 访问权限 |
| ---------- | ------ | ----------------------- | ---- | ---- |
| 加入购物车     | POST   | `/api/cart/items`       | productId, quantity | USER / MERCHANT / ADMIN |
| 购物车列表     | GET    | `/api/cart/items`       | 无 | USER / MERCHANT / ADMIN |
| 修改数量       | PUT    | `/api/cart/items/{id}/quantity` | quantity | USER / MERCHANT / ADMIN |
| 勾选/取消勾选   | PUT    | `/api/cart/items/{id}/selected` | selected（0/1） | USER / MERCHANT / ADMIN |
| 删除条目       | DELETE | `/api/cart/items/{id}`  | id | USER / MERCHANT / ADMIN |
| 汇总信息       | GET    | `/api/cart/summary`     | 无 | USER / MERCHANT / ADMIN |

---

## 六、订单模块接口

---

| 接口名称       | 请求方式 | 接口路径           | 参数说明 | 访问权限 |
| ---------- | ---- | -------------- | ---- | ---- |
| 从购物车创建订单 | POST | `/api/orders`  | addressId | USER / MERCHANT / ADMIN |
| 订单分页列表   | GET  | `/api/orders`  | page, size, status（可选） | USER / MERCHANT / ADMIN |
| 订单详情       | GET  | `/api/orders/{id}` | id | USER / MERCHANT / ADMIN |
| 取消订单       | PUT  | `/api/orders/{id}/cancel` | id | USER / MERCHANT / ADMIN |

---

## 七、支付模块接口

---

| 接口名称             | 请求方式 | 接口路径                     | 参数说明 | 访问权限 |
| ---------------- | ---- | ------------------------ | ---- | ---- |
| 创建支付单           | POST | `/api/payments`          | orderId, channel（可选） | USER / MERCHANT / ADMIN |
| 查询支付单状态         | GET  | `/api/payments/{id}`     | id | USER / MERCHANT / ADMIN |
| 模拟收银台动作确认/取消 | POST | `/api/payments/{id}/mock-pay` | action（CONFIRM/CANCEL） | USER / MERCHANT / ADMIN |

---

## 八、文件管理模块接口

---

| 接口名称 | 请求方式 | 接口路径            | 参数说明         | 访问权限 |
| ---- | ---- | --------------- | ------------ | ---- |
| 上传文件 | POST | `/api/file/upload` | multipart/form-data: file | USER / MERCHANT / ADMIN |

---

## 九、智能定制模块接口（AI 生图）

> 📌 说明：以下接口需要登录（JWT）。请求头携带：`Authorization: Bearer <token>`。

| 接口名称     | 请求方式 | 接口路径                               | 参数说明 | 访问权限 |
| ---------- | ---- | ---------------------------------- | ---- | ---- |
| 文生图       | POST | `/api/customize/ai-image/text-to-image` | JSON Body：prompt（必填）, resolution（可选）, seed（可选）, style（可选）, rspImgType（可选） | USER / MERCHANT / ADMIN |
| 图生图       | POST | `/api/customize/ai-image/image-to-image` | multipart/form-data：file（必填）, prompt（必填）, negativePrompt（可选）, strength（可选）, resolution（可选）, enhanceImage（可选）, restoreFace（可选）, styles（可选）, count（可选）, rspImgType（可选） | USER / MERCHANT / ADMIN |
| 历史生图记录分页 | GET  | `/api/customize/ai-image/records`        | page, size | USER / MERCHANT / ADMIN |

---

## 十、定制渠道模块接口

> 📌 说明：以下接口需要登录（JWT）。工单操作需校验参与方权限（发起用户或被选商家）。

### 10.1 商家列表

| 接口名称 | 请求方式 | 接口路径 | 参数说明 | 访问权限 |
| ---- | ---- | ---- | ---- | ---- |
| 可选商家列表 | GET | `/api/customize/merchants` | 无（聚合商家+商品信息） | USER |

### 10.2 定制工单

| 接口名称 | 请求方式 | 接口路径 | 参数说明 | 访问权限 |
| ---- | ---- | ---- | ---- | ---- |
| 发起定制请求 | POST | `/api/customize/requests` | merchantId, title, description, imageUrl（可选） | USER |
| 我的定制列表 | GET | `/api/customize/requests/mine` | page, size, status（可选） | USER |
| 收到的定制列表 | GET | `/api/customize/requests/received` | page, size, status（可选） | MERCHANT |
| 工单详情 | GET | `/api/customize/requests/{id}` | id | USER / MERCHANT |
| 商家报价 | PUT | `/api/customize/requests/{id}/quote` | quotedPrice | MERCHANT |
| 用户确认报价 | PUT | `/api/customize/requests/{id}/confirm` | addressId | USER |
| 更新工单状态 | PUT | `/api/customize/requests/{id}/status` | status（COMPLETED / CANCELLED） | USER / MERCHANT |

### 10.3 沟通消息

| 接口名称 | 请求方式 | 接口路径 | 参数说明 | 访问权限 |
| ---- | ---- | ---- | ---- | ---- |
| 发送消息 | POST | `/api/customize/requests/{requestId}/messages` | content | USER / MERCHANT |
| 消息列表 | GET | `/api/customize/requests/{requestId}/messages` | 无 | USER / MERCHANT |

---

## 十一、非遗文化模块接口

---

### 11.1 前台公开接口（面向展示）

| 接口名称   | 请求方式 | 接口路径                           | 参数说明                             | 访问权限 |
| ------ | ---- | ---------------------------------- | -------------------------------- | ---- |
| 获取分类树  | GET  | `/api/heritage/categories/tree`    | 无                                | 所有人  |
| 项目分页列表 | GET  | `/api/heritage/projects`           | page, size, keyword（可选）, categoryId（可选） | 所有人  |
| 项目详情   | GET  | `/api/heritage/projects/{id}`      | id                               | 所有人  |
| 传承人详情  | GET  | `/api/heritage/inheritors/{id}`    | id                               | 所有人  |

---

### 11.2 管理端接口（管理员）

| 接口名称   | 请求方式 | 接口路径                               | 参数说明                                                     | 访问权限 |
| ------ | ---- | ---------------------------------- | -------------------------------------------------------- | ---- |
| 获取分类树  | GET  | `/api/admin/heritage/categories/tree` | 无                                                       | ADMIN |
| 新增类型   | POST | `/api/admin/heritage/categories`      | name, parentId（可选）, sortOrder（可选，默认 0）                   | ADMIN |
| 项目分页列表 | GET  | `/api/admin/heritage/projects`        | page, size, keyword（可选）, categoryId（可选）                  | ADMIN |
| 项目详情   | GET  | `/api/admin/heritage/projects/{id}`   | id                                                       | ADMIN |
| 新增项目   | POST | `/api/admin/heritage/projects`        | categoryId, name, description, applyUnit, protectUnit     | ADMIN |
| 修改项目   | PUT  | `/api/admin/heritage/projects/{id}`   | categoryId, name, description, applyUnit, protectUnit     | ADMIN |
| 更新项目传承人 | PUT  | `/api/admin/heritage/projects/{id}/inheritors` | inheritorIds[]                                           | ADMIN |
| 新增项目媒体 | POST | `/api/admin/heritage/projects/{id}/medias` | mediaType, mediaUrl, sortOrder（可选，默认 0）                  | ADMIN |
| 更新项目媒体 | PUT  | `/api/admin/heritage/projects/medias/{mediaId}` | mediaType（可选）, mediaUrl（可选）, sortOrder（可选）            | ADMIN |
| 删除项目媒体 | DELETE | `/api/admin/heritage/projects/medias/{mediaId}` | mediaId                                                  | ADMIN |
| 删除项目   | DELETE | `/api/admin/heritage/projects/{id}` | id                                                       | ADMIN |
| 传承人分页列表 | GET  | `/api/admin/heritage/inheritors`      | page, size, keyword（可选）                                  | ADMIN |
| 传承人详情  | GET  | `/api/admin/heritage/inheritors/{id}` | id                                                       | ADMIN |
| 新增传承人  | POST | `/api/admin/heritage/inheritors`      | name, gender（可选）, birthDate（可选）, nation（可选）, photo（可选）, description（可选） | ADMIN |
| 修改传承人  | PUT  | `/api/admin/heritage/inheritors/{id}` | name, gender（可选）, birthDate（可选）, nation（可选）, photo（可选）, description（可选） | ADMIN |
| 删除传承人  | DELETE | `/api/admin/heritage/inheritors/{id}` | id                                                       | ADMIN |

---

## 十二、接口设计总结（论文可直接用）

本系统后端接口采用 RESTful 风格进行设计，根据不同功能模块划分接口路径，并结合用户角色实现访问权限控制。接口命名语义清晰、结构统一，有利于前后端分离开发及系统后期维护与扩展。

---

## 十三、变更记录

| 变更日期    | 变更内容 |
| ----------- | -------- |
| 2026-01-28  | 清理未实现接口，按当前代码更新认证/用户/商品/实名认证/文件上传接口清单 |
| 2026-01-29  | 新增收货地址模块接口清单（新增/修改/删除/列表/设默认） |
| 2026-02-05  | 新增非遗文化模块接口清单（分类树/项目分页/项目详情/传承人详情），并开放 `/api/heritage/**` 匿名访问 |
| 2026-02-06  | 新增非遗管理端接口清单（管理员：分类树/类型新增/项目 CRUD/项目传承人更新/项目媒体增删改/传承人 CRUD） |
| 2026-02-09  | 新增智能定制 AI 生图接口清单（文生图/图生图/历史记录分页） |
| 2026-02-10  | 补充订单模块文档变更记录（购物车条目/订单/支付链路）并更新相关文档口径 |
| 2026-02-11  | 补充购物车/订单/支付接口清单，修正文件上传权限与 AI 生图参数 |
| 2026-04-17  | 新增定制渠道模块接口清单（商家列表/定制工单CRUD/沟通消息） |


