server:
  port: 8080

spring:
  application:
    name: heritage-shop
  
  autoconfigure:
    exclude:
  
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
  
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/develop?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 13655218Cc.
  
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 3000ms
      lettuce:
        pool:
          max-active: 8
          max-wait: -1ms
          max-idle: 8
          min-idle: 0
  
  profiles:
    active: mock

jwt:
  secret: heritage-shop-secret-key-2024
  expiration: 604800000

ai:
  tencentcloud:
    secret-id: ${TENCENTCLOUD_SECRET_ID:}
    secret-key: ${TENCENTCLOUD_SECRET_KEY:}
    region: ${TENCENTCLOUD_REGION:ap-guangzhou}
    endpoint: ${TENCENTCLOUD_ENDPOINT:aiart.tencentcloudapi.com}

knife4j:
  enable: true
  setting:
    language: zh_cn

file:
  upload:
    path: uploads
  public-base-url: ${PUBLIC_BASE_URL:}
